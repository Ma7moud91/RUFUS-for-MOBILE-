# Bootloaders Directory
Official bootloader binaries (e.g. UEFI Shell, FreeDOS) are downloaded on-demand with SHA-256 hash pinning and cached in the app's internal storage directory (filesDir/bootloaders/).
Pre-bundled binaries may also be placed in this assets folder as offline fallback.
